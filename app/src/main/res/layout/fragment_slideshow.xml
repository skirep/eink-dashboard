<?xml version="1.0" encoding="utf-8"?>
<!--
    Slideshow fragment – full-screen image that rotates every 3 hours.
    Shows a placeholder text when no images are available.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Full-screen slideshow image -->
    <ImageView
        android:id="@+id/ivSlideshow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        android:contentDescription="@string/page_slideshow" />

    <!-- Shown only when no images are found -->
    <TextView
        android:id="@+id/tvNoImages"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/no_images"
        android:textColor="@color/on_background"
        android:textSize="18sp"
        android:visibility="gone" />

</FrameLayout>
