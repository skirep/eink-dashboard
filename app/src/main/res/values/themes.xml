<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base e-ink theme: no animations, high contrast, no colour accent -->
    <style name="Theme.EInkLauncher" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:textColor">@color/on_background</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="colorPrimary">@color/eink_black</item>
        <item name="colorPrimaryDark">@color/eink_black</item>
        <item name="colorAccent">@color/eink_dark_gray</item>
    </style>

    <!-- Large, square navigation button for e-ink touch -->
    <style name="Widget.EInk.NavButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/button_background</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textSize">18sp</item>
        <item name="android:padding">16dp</item>
        <item name="android:minWidth">80dp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

</resources>
